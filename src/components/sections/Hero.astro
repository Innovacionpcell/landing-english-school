---
import Icon from '../Icon.astro';
import { whatsappUrl } from '../../config/site';
import { copy } from '../../config/copy';
const c = copy.hero;
// Si copy.promo tiene texto, la promo vigente se muestra como etiqueta amarilla en la tarjeta (y el primer ítem deja de resaltarse)
---
<section class="hero">
  <div class="container hero__grid">
    <div class="hero__copy">
      <p class="eyebrow">{c.eyebrow}</p>
      <h1>{c.h1a} <span class="hl">{c.h1b}</span></h1>
      <p class="lead">{c.sub}</p>
      <div class="hero__ctas">
        <a class="btn" href="#matricula" data-cta="hero">{c.cta1}</a>
        <a class="btn btn--wa" href={whatsappUrl()} target="_blank" rel="noopener" data-cta="hero-wa"><Icon name="whatsapp" />{c.cta2}</a>
      </div>
      <ul class="proof">
        {c.badges.map((b) => <li><Icon name="check" class="ok" />{b}</li>)}
      </ul>
    </div>
    <aside class="hero__card" aria-labelledby="hero-card-t">
      <span class="stripes" aria-hidden="true"></span>
      {copy.promo && <p class="hero__promo">{copy.promo}</p>}
      <p class="hero__card-t" id="hero-card-t">{c.cardTitle}</p>
      <ul>
        {c.cardItems.map((s, i) => <li class={i === 0 && !copy.promo ? 'is-hl' : undefined}><span><Icon name="check" /></span>{s}</li>)}
      </ul>
      <a class="btn hero__card-cta" href="#matricula" data-cta="hero-card">{c.cardCta}</a>
    </aside>
  </div>
</section>

<style>
  .hero { position: relative; overflow: hidden; background: #fff; border-bottom: 1px solid var(--line); padding-block: clamp(44px, 7vw, 96px) clamp(56px, 8vw, 104px); }
  .hero::after { content: ''; position: absolute; right: 0; top: 0; width: min(40vw, 520px); height: 100%; background: var(--bg); z-index: 0; }
  .hero__grid { position: relative; z-index: 1; display: grid; grid-template-columns: 1.2fr .8fr; gap: clamp(32px, 6vw, 72px); align-items: center; }
  .hl { color: var(--navy); }
  .hero .lead { max-width: 600px; margin-bottom: 28px; }
  .hero__ctas { display: flex; flex-wrap: wrap; gap: 12px; margin-bottom: 26px; }
  .proof { display: flex; flex-wrap: wrap; gap: 10px 22px; list-style: none; padding: 0; margin: 0; font-size: .95rem; color: var(--ink); font-weight: 500; }
  .proof li { display: inline-flex; align-items: center; gap: 8px; }
  .proof :global(.ok) { width: 18px; height: 18px; color: var(--navy); }

  .hero__card { position: relative; overflow: hidden; background: var(--navy); color: #e7e8f6; border-radius: 24px; padding: clamp(28px, 4vw, 40px); box-shadow: var(--shadow-lg); }
  /* Franjas de la bandera: detalle decorativo en rojo */
  .stripes { position: absolute; right: -30px; top: 30px; width: 110px; height: 42px; background: repeating-linear-gradient(180deg, var(--red) 0 8px, transparent 8px 16px); opacity: .9; transform: rotate(-8deg); }
  .hero__promo { position: relative; display: inline-block; margin: 0 0 14px; background: var(--yellow); color: var(--navy-deep); font-weight: 700; font-size: .9rem; border-radius: 999px; padding: 4px 12px; }
  .hero__card-t { position: relative; font-family: var(--font-head); font-weight: 700; font-size: 1.55rem; color: #fff; margin: 0 0 22px; line-height: 1.2; max-width: 78%; }
  .hero__card ul { list-style: none; padding: 0; margin: 0 0 28px; display: grid; gap: 14px; }
  .hero__card li { display: flex; align-items: center; gap: 12px; font-weight: 500; color: #fff; }
  .hero__card li span { flex: none; width: 28px; height: 28px; border-radius: 50%; display: grid; place-items: center; border: 2px solid rgb(255 255 255 / .35); }
  .hero__card li span :global(svg) { width: 16px; height: 16px; }
  .hero__card li.is-hl span { background: var(--yellow); border-color: var(--yellow); color: var(--navy-deep); } /* único acento amarillo */
  .hero__card-cta { width: 100%; }

  @media (max-width: 900px) {
    .hero::after { display: none; }
    .hero__grid { grid-template-columns: 1fr; }
  }
</style>
